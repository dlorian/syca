<h1>Details</h1>
<div id="formContainer">
	<div id="msgContainer">{{view EmberApp.MessageView}}</div>
	<form {{action 'save' on='submit'}}>
		<div class="row">
			<div class="col-xs-12">
				<div class="form-group">
					<label for="date">Datum</label>
					{{view EmberApp.DateField date=date id="date" disabled=isDisabled}}
				</div>
			</div>
		</div>
		<div class="row">
			<div class="col-xs-6">
				<div class="form-group">
					 <label for="totalKm">Strecke gesamt [km]:</label>
					{{view Ember.TextField value=totalKm id="totalKm" class="form-control" disabled=isDisabled 
					pattern="^[0-9]{1,3}((\.|\,)[0-9]{1,2})?$"}}
				</div>
				<div class="form-group">
					<label for="avgSpeed"> Avg. Geschwindigket</label>
					{{view Ember.TextField value=avgSpeed id="avgSpeed" class="form-control" disabled=isDisabled
					pattern="^[0-9]{1,3}((\.|\,)[0-9]{1,2})?$"}}
				</div>
				<div class="form-group">
					<label for="topSpeed"> Top. Geschwindigket</label>
					{{view Ember.TextField value=topSpeed id="topSpeed" class="form-control" disabled=isDisabled
					pattern="^[0-9]{1,3}((\.|\,)[0-9]{1,2})?$"}}
				</div>
			</div><!-- /.col-xs-6 -->
			<div class="col-xs-6">
				<div class="form-group">
					<label for="totalTime">Zeit gesamt:</label>
					{{view Ember.TextField value=totalTime id="totalTime" class="form-control" disabled=isDisabled pattern="^([0-9]{2}\:)?[0-9]{2}\:[0-9]{2}$"}}
				</div>
				<div class="form-group">
					<label for="time20">Zeit bei 20km:</label>
					{{view Ember.TextField value=time20 id="time20" class="form-control" disabled=isDisabled
					pattern="^([0-9]{2}\:)?[0-9]{2}\:[0-9]{2}$"}}
				</div>
				<div class="form-group">
					<label for="time30">Zeit bei 30km:</label>
					{{view Ember.TextField value=time30 id="time30" class="form-control" disabled=isDisabled
					pattern="^([0-9]{2}\:)?[0-9]{2}\:[0-9]{2}$"}}
				</div>
			</div><!-- /.col-xs-6 -->
		</div><!-- /.row -->
		<div class="row">
		 	<div class="col-xs-6">
				<div class="form-group">
				    <label for="condition">Bedingungen:</label>
				    {{view Ember.Select content=EmberApp.weatherCondition id="condition" class="form-control" disabled=isDisabled}}
				</div>
				<div class="form-group">
				    <label for="temperature">Temperatur:</label>
				    {{view Ember.TextField value=temperature  id="temperature" class="form-control" disabled=isDisabled
				    pattern="^[0-9]{1,2}((\.|\,)[0-9]{1,2})?$"}}
				</div>
				<div class="form-group">
				    <label for="windDirection">Windrichtung:</label>
				    {{view Ember.Select content=EmberApp.windDirection id="windDirection" class="form-control" disabled=isDisabled}}
				</div>
			</div><!-- /.col-xs-6 -->
			<div class="col-xs-6">
				<div class="form-group">
				    <label for="windSpeed">Windgeschwindigkeit:</label>
				    {{view Ember.TextField value=windSpeed id="windSpeed" class="form-control" disabled=isDisabled
				    pattern="^[0-9]{1,2}$"}}
				</div>
				<div class="form-group">
				    <label for="windStrength">Windstärke:</label>
				    {{view Ember.TextField value=windStrength id="windStrength" class="form-control" disabled=isDisabled pattern="^[0-9]{1,2}$"}}
				</div>
				<div class="form-group">
				    <label for="windBlasts">Böen:</label>
				    {{view Ember.TextField value=windBlasts id="windBlasts" class="form-control" disabled=isDisabled
				    pattern="^[0-9]{1,2}$"}}
				</div>
			</div><!-- /.col-xs-6 -->
		</div><!-- /.row -->
		<div class="row">
			<div class="col-xs-12 col-md-12">
				<div class="form-group">
				    <label for="description">Beschreibung der Strecke</label>
				    {{view Ember.TextArea value=description id="description" class="form-control" placeholder="Wegpunkte..." disabled=isDisabled}}
				</div>
			</div><!-- /.col-md-8-->
		</div><!-- /.row -->
		<div>
			<button class="btn btn-default" {{action 'setEditable'}} {{bindAttr hidden="isNotDisabled"}}>Ändern</button>
			<button class="btn btn-default" type="submit" {{bindAttr hidden="isDisabled"}} {{bindAttr disabled="isNotDirty"}}>Speichern</button>
		</div>
	</form>
</div>